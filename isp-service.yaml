apiVersion: v1
kind: Service
metadata:
  name: isp-loadbalancer
spec:
  selector:
    app: isp-optimizer 
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 5000  
  type: LoadBalancer